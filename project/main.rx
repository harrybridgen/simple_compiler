struct Player {
    x = 0;
    y = 0;
    xy ::= x + y;
}

func makeplayer(nx, ny) {
    p := struct Player;
    p.x = nx;
    p.y = ny;
    return p;
}

func main(){
    player := makeplayer(10, 5);
    player.x = 5;
    println player.xy; # 10 #
}